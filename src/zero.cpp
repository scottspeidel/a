#include <vector>
#include <iostream>

#include "zero.h"

void Zero::move_zeros(std::vector<int> &nums){
  int *s = &nums[0];
  int *e = s + nums.size();
  int temp;

  std::cout << "move zeros"<<std::endl;

};
